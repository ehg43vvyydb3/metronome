# metronome

서로 독립된 단일 HTML 파일들(simple / score-player / backing-track-sync-editor / rhythm). 빌드 없음, 서버 없음, 프레임워크 없음 — 함수 단위로 작성.

## simple.html

Web Audio API의 lookahead 스케줄러 패턴(setTimeout으로 25ms마다 깨어나서 향후 100ms 안의 노트를 미리 스케줄)으로 박자를 맞춘다. 이 패턴은 score-player.html에도 그대로 재사용된다.

## score-player.html

기타 탭 PDF를 마디 단위로 하이라이트하며 재생하는 연습용 메트로놈. 설계 히스토리와 이유:

- **PDF는 score-player.html 안에서 변환하지 않고, 미리 PNG로 변환해서 쓴다.** 로컬의 다른 프로젝트 [MathFlow](https://github.com/../MathFlow)(스캔 교과서를 블록 단위로 잘라 보여주는 뷰어)가 "무거운 처리는 미리 끝내고, 뷰어는 이미지 좌표만 다룬다"는 구조를 쓰고 있어서 그대로 차용했다. `pdf_to_png.py`(터미널) 또는 `pdf_to_png_gui.command`(더블클릭용 tkinter GUI, 내부적으로 `pdf_to_png.py`의 `convert()`를 그대로 재사용)로 `scores/곡이름/page-N.png`를 한 번만 만들어두면, score-player.html에서는 "폴더 불러오기"로 그 폴더를 선택하기만 하면 된다. 원래는 브라우저 안에 pdf.js 기반 변환 버튼도 있었지만, 폴더 자동 저장에 쓰던 File System Access API(`showDirectoryPicker`)가 `file://`로 열면 아예 동작하지 않고 Chrome/Edge에서도 보안 컨텍스트(HTTPS/localhost)가 필요해서 계속 혼란을 일으켰다. 브라우저 API에 의존하지 않는 쪽이 이 프로젝트의 "간단하게" 원칙에 맞아서 걷어내고 지금 구조로 정리했다.
- **마디 경계는 자동 인식하지 않고 한 번만 수동 지정한다.** 벡터 탭 PDF에서 마디선을 코드로 안정적으로 찾아내는 건 OMR 수준의 복잡도라 "간단하게 구현" 원칙에 안 맞는다. 대신 곡마다 한 번, 드래그(줄 영역) + 클릭(마디 경계)으로 지정하고 나면 이후엔 반복 재생만 하면 된다. 지정한 좌표는 원본 이미지 픽셀 기준으로 저장되어 렌더링 배율과 무관하다.
- **반복 기호(D.C., Play 2x 등)는 처리하지 않는다.** 악보에 그려진 순서(위→아래, 페이지순)대로만 재생. 리피트가 있으면 해당 줄을 다시 한번 마디 지정해서 순서에 끼워 넣어야 한다 — 의도적으로 안 만든 기능이니 "왜 반복 기호를 못 읽지"라는 질문이 나오면 이게 답이다.
- **마디 지정·BPM 등 설정은 브라우저와 무관하게 전부 localStorage에만 저장한다.** 곡 이름(폴더 이름) 기준으로 저장되므로, 같은 폴더를 다시 불러오면 자동으로 복원된다. 과거엔 File System Access API로 폴더 안 meta.json에도 저장을 시도했지만(Chrome/Edge 전용, `file://`에서는 아예 동작 안 함) 위 이유로 걷어냈다 — 지금은 브라우저·프로토콜 무관하게 동일하게 동작한다. "최근 열기" 목록도 마찬가지로 곡 이름만 localStorage에 기록해두고, 클릭하면 폴더 재선택 창을 띄우는 방식이다.
  - **웹페이지가 로컬 폴더에 파일을 자동으로 쓰는 표준 방법은 File System Access API뿐**이라, 이걸 뺀 이상 폴더 안에 meta.json을 자동 저장하는 건 원천적으로 불가능하다(브라우저를 가리지 않는 `fetch` 기반 로컬 서버를 새로 만들지 않는 한). "서버 없음" 원칙을 지키기 위해 의도적으로 포기한 트레이드오프다.
  - **데이터가 폴더(파일)가 아니라 "곡 이름" 문자열에 묶여 있다.** `scores/곡이름/`의 PNG를 지우고 같은 이름으로 새로 변환해 넣어도 마디 정보는 그대로 남아있다(파일이 아니라 브라우저 localStorage를 보는 거라서). 반대로 브라우저 데이터를 지우면 폴더는 멀쩡해도 마디 정보는 사라진다. 완전히 새로 시작하려면 마디 지정 화면의 "전체 초기화" 버튼을 눌러야 한다 — "폴더를 새로 만들었는데 왜 예전 마디가 남아있지"라는 질문이 나오면 이게 답이다.
- **재생 중 3줄 창은 한 줄씩이 아니라 두 줄씩 넘어간다.** 처음엔 한 줄 전환마다 스크롤했는데 너무 산만하다는 피드백을 받고, 창의 첫 두 줄을 재생하는 동안은 그대로 두고 세 번째 줄(미리보기로 보이던 줄)로 넘어가는 순간에만 그 줄이 새 창의 첫 줄이 되도록 페이징으로 바꿨다(`windowStartLine`). 이 전환에는 위로 슬라이딩하는 애니메이션이 붙어 있다 — 재생 중 자동 전환에만 적용되고, 클릭/입력으로 직접 마디를 옮길 때는 즉시 반응하도록 애니메이션 없음.
- **"처음으로" 버튼은 1번 마디가 아니라 선택된 재생 구간의 시작 마디로 간다.** 일부 재생(once1~3)으로 특정 구간을 연습하는 중에 "처음으로"가 곡 맨 앞으로 튀면 구간 연습이 끊기기 때문에, `getLoopBounds().start0`으로 이동한다("전체 재생"이면 결과적으로 1번 마디).
- **스페이스바 재생/정지: 텍스트 입력·체크박스·select·button에 포커스가 있을 때만 예외.** 숫자 입력(BPM, 박자, 마디 이동 등)은 스페이스를 쓰지 않으므로 포커스가 있어도 스페이스가 재생을 토글한다 — 사용자가 명시적으로 요청한 동작이니 "숫자 입력에서도 스페이스가 먹히는게 버그 아닌가" 하고 막지 말 것.

## backing-track-sync-editor.html

score-player.html의 마디 지정/재생과 배킹트랙(실제 음원) 재생을 합친 도구. score-player.html은 손대지 않고 그대로 남겨뒀다 — 배킹트랙 없이 클릭 메트로놈만으로 연습하는 곡은 계속 score-player.html을 쓴다. 설계 히스토리와 이유:

- **오디오 파일도 PNG처럼 `scores/곡이름/` 폴더 안에 미리 넣어둔다.** `pdf_to_png.py`가 PDF를 미리 PNG로 변환해 폴더에 저장해두는 것과 같은 패턴을, 오디오에도 그대로 적용했다. 오디오는 변환이 필요 없으니 Finder로 드래그해 폴더에 복사하면 끝이다. "폴더 불러오기" 한 번으로 `page-N.png`와 오디오 파일(mp3/wav/m4a 등, 확장자로 인식)을 동시에 읽어온다 — 매 세션 오디오를 따로 불러오는 절차를 없애기 위한 선택이다. 폴더에 오디오가 2개 이상이면 파일명 알파벳순 첫 번째를 그냥 쓴다(선택 UI를 따로 만들지 않았다).
- **File System Access API 기반 자동 저장("프로젝트 폴더 연결")은 만들지 않았다.** score-player.html이 이미 걷어낸 것과 같은 이유(브라우저·프로토콜 의존, "서버 없음" 원칙)로, 오디오 싱크 값(BPM/박자/준비박/시작점)과 탭 마디(`lines`)를 전부 `localStorage`에만 저장한다. 저장 키는 score-player.html과 동일하게 탭 이미지 폴더 이름(songName) 기준이라, 오디오와 탭이 같은 로드 한 번으로 함께 복원된다.
- **재생은 합성 클릭이 아니라 실제 배킹트랙 오디오가 시간을 준다.** score-player.html의 setTimeout 기반 lookahead 클릭 스케줄러를 걷어내고, `시작점(startTime) + 마디번호 × 마디길이`로 배킹트랙 재생 시각을 마디 인덱스에 직접 매핑했다(`measureStartTime`). 그래서 음표쪼개기(subdivision)·준비마디(leadInMeasures)·클릭음 선택 같은 "클릭 전용" 개념은 전부 제거하고, 배킹트랙 오디오 자체의 준비박 구간(`countInBeats`)으로 대체했다.
- **재생 화면의 파형은 탭 줄 전체를 하나로 늘리지 않고, 마디 단위로 잘라 각 마디 폭에 맞춰 그린다.** 탭 표기 폭은 음표 밀도에 따라 마디마다 들쭉날쭉하기 때문에, 줄 전체를 시간 축으로 균일하게 늘리면 마디 경계와 파형이 어긋난다. 대신 마디별로 해당 시간 구간의 피크를 뽑아 그 마디의 실제 픽셀 폭에 맞춰 그리므로, 마디 경계선이 항상 파형 경계와 일치한다. 마디별 피크는 캐시해뒀다가 BPM/박자/시작점이 바뀌거나 마디가 재지정될 때만 다시 계산한다(매 프레임 오디오 전체를 다시 스캔하면 끊긴다).
- **연습 속도는 `playbackRate`가 아니라 미리 변환해둔 느린 음원으로 낸다 — 음정이 변하면 안 되기 때문이다.** 처음엔 `AudioBufferSourceNode.playbackRate`로 배속을 줬는데, 그러면 테이프를 느리게 돌리는 것처럼 음정까지 낮아져서 기타 튜닝과 어긋난다(연습용으로는 치명적). 브라우저에서 음정을 유지한 채 늘이려면 위상 보코더를 직접 짜거나 외부 라이브러리를 받아와야 해서, `pdf_to_png.py`와 같은 패턴으로 옮겼다: `stretch_audio.py`(librosa `effects.time_stretch`)로 `scores/곡이름/speed-80.wav` 같은 파일을 미리 만들어두면, 앱은 그걸 **정상 속도로 그냥 재생**한다(`playbackRate`는 항상 1).
  - **그래서 연습 속도는 폴더에 실제로 있는 speed-N 파일 중에서만 고를 수 있다.** 임의의 BPM을 즉석에서 만들 수 없으니 자유 입력 대신 버튼 목록으로 두는 게 정직하다(버튼 라벨에 `80% · 96 BPM`처럼 결과 BPM을 같이 보여준다). 느린 파일은 처음 선택할 때 디코딩하고 캐시한다(로드 때 전부 디코딩하면 느리고 메모리도 많이 먹는다).
  - **마디↔시간 매핑은 전부 "원본 오디오 안의 시간" 기준이라 연습 속도와 무관하다.** 느린 파일은 같은 내용을 1/rate배 길이로 늘여둔 것이라, 원본 시간 t는 그 파일 안에서 t/rate 지점에 있다. 그래서 속도를 반영해야 하는 곳은 `playFrom`의 두 줄(`bufferOffset = offset/rate`, `delay = -offset/rate`)과 `getCurrentTime`의 환산(`* currentRate`)뿐이고, `computePlaybackState`·`measureStartTime`·파형 피크·하이라이트는 손대지 않아도 그대로 맞는다. 파형과 피크는 언제나 원본 버퍼에서 뽑는다(느린 파일은 재생에만 쓴다).
  - 이 구조 덕분에 준비박 길이도 자동으로 연습 템포를 따라간다(120BPM 곡을 80%로 연습하면 4박 카운트인이 2초가 아니라 2.5초 = 96BPM의 4박).
  - 속도를 재생 중에 바꾸면 지금 마디부터 다시 시작한다. 재생 중인 소스는 어차피 버려야 하고, 준비박 대기 중에는 이미 예약된 시작 시각이 옛 속도 기준이라 다시 거는 쪽이 항상 정확하다.
  - `speed-N.*`은 메인 오디오 후보에서 제외한다 — 안 그러면 파일명 알파벳순으로 느린 버전이 원본으로 뽑힐 수 있다.
- **줄 전환 슬라이드 애니메이션에는 `slideAnimating` 가드가 반드시 필요하다.** 애니메이션 코드(`animateLineSlide`) 자체는 score-player.html에서 그대로 가져왔지만, 이 파일은 score-player와 달리 재생 중 매 프레임 도는 rAF 루프가 있다(플레이헤드를 마디 안에서 부드럽게 움직여야 해서). 그 루프가 매 프레임 `renderPlayWindow()`로 같은 캔버스를 다시 그리기 때문에, 가드가 없으면 슬라이드가 그린 중간 프레임이 즉시 덮어써져 애니메이션이 아예 보이지 않는다. 그래서 애니메이션이 도는 동안 `renderPlayWindow()`를 no-op으로 만들고(`slideAnimating`), 끝나면 `.then(renderPlayWindow)`로 최종 상태를 한 번 그린다. 같은 이유로 슬라이드 도중에 다음 줄 전환이 겹쳐 들어오면 새 애니메이션을 시작하지 않는다(중첩되면 서로를 덮어쓴다). 애니메이션 중 320ms 동안은 마디 하이라이트와 플레이헤드가 잠깐 사라지는데, 이건 score-player도 마찬가지인 의도된 동작이다.
- **"전체화면 연습" 모드는 재생/정지 버튼과 탭+파형만 남긴다.** 실제 연주하면서 볼 때는 폴더 로더·BPM 입력·마디 지정 도구 같은 설정 UI가 방해가 되므로, 브라우저 전체화면(Fullscreen API)으로 전환하면서 이런 UI를 전부 숨긴다. 구간 반복·재생 범위 같은 세부 설정은 전체화면 진입 전에 "재생" 탭에서 미리 끝내두는 걸 전제로 한다. 재생용 캔버스 폭도 이때만 고정 760px 대신 창 너비에 맞춰 커진다(마디 지정용 캔버스는 좌표 정밀도 때문에 항상 760px 고정).
- **자동 분석은 BPM만 한다 — 시작점은 항상 사람이 파형을 클릭해서 정한다.** 처음엔 온셋 자기상관을 JS로 직접 구현했지만 옥타브 오류(2배/0.5배 오인식) 보정이 약해서, `librosa.beat.beat_track`으로 `pdf_to_png.py`와 같은 패턴(무거운 처리는 미리 끝내고 뷰어는 결과만 읽는다)으로 옮겼다. 시작점까지 자동으로 잡아보려는 시도(무음 트림, 준비박 개수만큼 비트 그리드 건너뛰기)도 해봤지만, 준비박 유무나 `beat_track`의 트리밍 옵션 같은 것 때문에 한두 박씩 어긋나는 사례가 실제로 나왔다 — BPM만 맞으면 시작점은 파형을 보고 클릭 한 번으로 정확히 잡을 수 있으니, 자동 추정의 오차 가능성을 감수할 이유가 없었다. 그래서 `analyze_audio.py`는 BPM만 `audio_meta.json`에 써두고, backing-track-sync-editor.html은 폴더를 불러올 때 이 값이 있으면 BPM만 기본값으로 채운다(이미 저장된 설정이 있으면 자동 덮어쓰지 않고 "분석값 적용" 버튼으로 수동 반영). 시작점(`startTime`)은 이 파일이 관여하지 않는 값이다.
- **준비박(카운트인)이 들어갈 시간이 오디오 파일 맨 앞에 부족해도, 파일에 무음을 덧붙이지 않는다.** 처음엔 "앞에 무음 추가 → 패딩된 WAV 다운로드" 기능이 있었지만, `AudioBufferSourceNode.start(when, offset)`이 미래 시각(`when`)에 재생을 예약할 수 있다는 걸 활용해 더 간단하게 풀었다: 준비박 시작 시각이 파일의 0초보다 앞이면(`countInStartTime() < 0`), 그 부족분만큼 실제 재생 시작을 뒤로 예약하고 파일은 정확히 0초부터 재생한다(`playFrom`의 `delay`/`bufferOffset` 분리). 재생 위치를 나타내는 가상 시계(`playOffset`)가 음수에서 시작해 0을 지나가도록 그대로 두면 준비박 카운트다운·하이라이트 로직(`computePlaybackState`)이 별도 분기 없이 그대로 맞는다 — 그래서 오디오 파일을 수정할 필요 자체가 없어졌다.

### 참고

- `pdf_to_png.py`/`pdf_to_png_gui.py`는 `.venv`(pymupdf, `requirements.txt`)가 필요하다. GUI는 tkinter를 쓰는데, Homebrew python은 기본적으로 `_tkinter`가 빠져 있어서 `brew install python-tk@<버전>`이 별도로 필요하다.
- `analyze_audio.py`/`stretch_audio.py`도 같은 `.venv`/`requirements.txt`를 쓴다(librosa, numpy/scipy/numba 등이 딸려와서 첫 설치가 좀 걸린다).
- **`stretch_audio.py`의 사본은 원본과 같은 포맷으로 저장한다.** 처음엔 전부 WAV로 뽑았는데, 원본이 mp3인 경우가 대부분이라 사본 하나가 원본의 10배가 넘어 감당이 안 됐다. 지금은 확장자를 보고 libsndfile이 인코딩할 수 있는 포맷(wav/mp3/flac/ogg/aiff)이면 그대로 쓰고, m4a·aac처럼 인코딩을 지원하지 않는 포맷만 mp3로 대신 저장한다. 기본 10개를 만들면 사본이 원본보다 길어지는 만큼(50%면 두 배) 합계는 원본 파일의 약 15배가 된다. `scores/`는 git에 안 올라가므로 그대로 두되, 부담되면 필요한 속도만 인자로 넘기면 된다.
  - 여기서 쓰는 확장자 목록은 HTML의 `AUDIO_EXT_RE`/`SPEED_FILE_RE`와 맞춰야 한다 — 한쪽에만 추가하면 파이썬이 만든 파일을 앱이 못 읽는다.
- `scores/`는 저작권이 있는 악보 이미지가 생기는 폴더라 git에 포함하지 않는다(`.gitignore`).
